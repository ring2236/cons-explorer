# OpenAI-compatible narrative provider. Leave blank to use the deterministic demo narrative.
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=

# Optional: required only to enable the Cloudflare D1 narrative cache in a
# direct Workers deployment. Set the same non-secret build variable in Workers Builds.
CLOUDFLARE_D1_DATABASE_ID=
